{
  "name": "render-localtunnel-server",
  "version": "1.0.0",
  "private": true,
  "description": "Self-hosted LocalTunnel server for Gmail add-on tunneling",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "localtunnel-server": "^1.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
